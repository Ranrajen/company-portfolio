# Optional — only needed once you build a real backend.
# Until then, the contact form fails over to a demo "success" state
# and every other page reads from src/data/company.js directly.
VITE_API_BASE_URL=http://localhost:5000/api
